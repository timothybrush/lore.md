{
  "name": "lore-md",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsgo --noEmit",
    "format": "oxfmt --write package.json tsconfig.json wrangler.toml src test README.md docs",
    "format:check": "oxfmt --check package.json tsconfig.json wrangler.toml src test README.md docs",
    "lint": "pnpm format:check && pnpm typecheck && oxlint --deny-warnings --type-aware --tsconfig tsconfig.json src test",
    "check": "pnpm lint && pnpm test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260503.1",
    "@typescript/native-preview": "7.0.0-dev.20260503.1",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "oxlint-tsgolint": "^0.22.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  },
  "packageManager": "pnpm@10.33.2"
}
